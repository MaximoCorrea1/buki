node_modules/
dist/
*.log
.env
.env.local

# Tesseract downloads its language model into the working directory when run
# from Node. Regenerable, ~5MB - never commit it.
*.traineddata
*.traineddata.gz
.gstack/

.vercel
.context/

# Scratch, including tools/popup-harness.mjs output.
zzz-*

# Design sources. Not shipped: the extension package is this folder, so a 264KB
# reference PNG would ride along into every install. The mark itself is GEOMETRY in
# docs/index.html, popup.html and options.html; docs/brand.md records the coordinates.
design/
